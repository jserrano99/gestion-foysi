{% extends "layout.html.twig" %} 
{% block titulo %} GESTIÓN DE SERVICIOS 
    {% if accion == 'I' %}
        ** NUEVO SERVICIO **
    {% else %}
        MODIFICACION : {{servicio.descripcion}}
    {% endif %}
    <hr/>
    <a href="{{path("queryServicio")}}" class="btn btn-success">Volver</a>
{% endblock %}

{% block contenido %}
    <div class="container">
        {% for message in app.session.flashbag().get('status') %}
            <div class="alert alert-success">
                {{message}}
            </div>
        {% endfor %}
    </div>
    <div class="container">
        {{form_start (form,{'action':'','method':'POST'})}}
        {{form_end (form)}}
    </div>
    <div class="clearfix"> </div>
{% endblock %}